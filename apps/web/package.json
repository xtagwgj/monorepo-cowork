{
  "name": "@cowork/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit",
    "lint": "echo \"skip lint for web\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@cowork/ui": "workspace:*",
    "@cowork/utils": "workspace:*",
    "@vueuse/core": "catalog:",
    "pinia": "catalog:",
    "shadcn-vue": "^0.11.3",
    "vue": "catalog:",
    "vue-router": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitejs/plugin-vue": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "vue-tsc": "catalog:"
  }
}
